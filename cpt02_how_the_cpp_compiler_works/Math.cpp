#if 1
// 使用int替换INTEGER
#define INTEGER int

INTEGER Multiply(INTEGER a, INTEGER b)
{
    INTEGER result = a * b;
    return result;
// #include只是把其他文件的内容简单粘贴进来，这里缺失的大括号由"EndBrace.h"补全
#include "EndBrace.h"

#endif

// if 语句
#if 0
asdasda
#endif

// 将iostream源码完全粘贴进来
#include <iostream>
